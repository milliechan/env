import React, { PropTypes } from 'react'

class UserFinalize extends React.Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <div>User Finalize</div>
        )
    }
};

export default UserFinalize;
